#!/usr/bin/env node

var commands = require('commander');


if (process.argv.length === 2) {
  process.argv.push('--help');
}

commands
  .version(require('../package.json').version)
  .command('new [name]', 'Make a subdirectory called <name> and create a new Meteor app there.')
  .parse(process.argv);
